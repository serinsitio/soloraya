import 'package:flutter/material.dart';

class UserPemesanan extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
          'Pemesanan',
          style: TextStyle(fontSize: 40),
        ),
      );
  }
}